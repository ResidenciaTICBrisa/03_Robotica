cmake_minimum_required(VERSION 2.8)
project(hello-world)

find_package(qibuild)

# Create a executable named hello-world
# with the source file: main.cpp
qi_create_bin(hello-world "main.cpp")

# Add a simple test:
qi_create_test(test_hello-world "test.cpp")
